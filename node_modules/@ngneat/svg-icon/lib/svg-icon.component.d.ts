import { ElementRef, SimpleChanges } from '@angular/core';
import { SvgIcons } from './types';
import { SvgIconRegistry } from './registry';
import { SVG_CONFIG } from './providers';
import * as i0 from "@angular/core";
export declare class SvgIconComponent {
    private host;
    private registry;
    private config;
    key: SvgIcons;
    fallback: SvgIcons;
    size: keyof SVG_CONFIG['sizes'];
    width: number | string;
    height: number | string;
    fontSize: number | string;
    color: string;
    noShrink: boolean;
    preserveAspectRatio: string | undefined;
    private mergedConfig;
    private lastKey;
    private init;
    constructor(host: ElementRef, registry: SvgIconRegistry, config: SVG_CONFIG);
    get element(): HTMLElement;
    ngOnChanges(changes: SimpleChanges): void;
    private createConfig;
    private setIconSize;
    private setIcon;
    static ɵfac: i0.ɵɵFactoryDeclaration<SvgIconComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<SvgIconComponent, "svg-icon", never, { "key": "key"; "fallback": "fallback"; "size": "size"; "width": "width"; "height": "height"; "fontSize": "fontSize"; "color": "color"; "noShrink": "noShrink"; "preserveAspectRatio": "preserveAspectRatio"; }, {}, never, never, true, never>;
}
