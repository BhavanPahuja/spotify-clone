import { GeneratorConfig } from './config';
export declare class SvgGeneratorWebpackPlugin {
    private config;
    constructor(config: GeneratorConfig & {
        watch?: boolean;
    });
    apply(): void;
}
