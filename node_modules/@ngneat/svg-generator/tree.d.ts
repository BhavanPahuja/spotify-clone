import { GeneratorConfig } from './config';
declare type VirtualFile = {
    path: string;
    content: string;
    name: string;
    identifierName: string;
};
declare type Config = Required<Omit<GeneratorConfig, 'srcPath' | 'outputPath'>>;
export declare const INDEX = "__INDEX__";
export declare function createTree(srcPath: string, outputPath: string, config: Config): VirtualFile[];
export declare function resolveIdentifierName(iconName: string, config: Config): string;
export {};
