#!/usr/bin/env node
"use strict";
var _a;
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const cosmiconfig_1 = require("cosmiconfig");
const generator_1 = require("./generator");
const program = new commander_1.Command();
program
    .name('svg-generator')
    .description('Transform SVG files into Angular-compatible TS files')
    .version('4.0.0')
    .option('-c, --config-dir <value>', 'Specify a directory for the config file (defaults to current working directory)', process.cwd());
program.parse();
const opts = program.opts();
const explorerSync = (0, cosmiconfig_1.cosmiconfigSync)('svgGenerator');
const config = (_a = explorerSync.search(opts.configDir)) === null || _a === void 0 ? void 0 : _a.config;
(0, generator_1.generateSVGIcons)(config);
