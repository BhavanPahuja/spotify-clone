export interface GeneratorConfig {
    srcPath: string;
    outputPath: string;
    svgoConfig: {
        plugins: any[];
    };
    prefix?: string;
    postfix?: string;
    rootBarrelFile?: boolean;
    rootBarrelFileName?: string;
    invalidCharReplacer?: (invalidChar: string) => string;
}
export declare const defaultConfig: Required<GeneratorConfig>;
